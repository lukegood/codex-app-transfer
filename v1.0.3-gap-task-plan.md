# v1.0.3 对照缺口任务推进文档

> 创建日期: 2026-05-05
> 当前基线: `src-tauri/Cargo.toml` 中的 `2.0.2`
> 对照基线: Git tag `v1.0.3`
> 状态: P0.0 已完成代码转译; P0.1 已完成代码核对与本地 OpenAI-compatible mock 回归验证; P0.2 已完成旧逻辑核对与本地 mock 回归验证; P0.3 已完成配置备份/导入代码核对与本地回归验证; P0.4 已完成旧逻辑核对与本地回归验证; P0.5 已完成旧逻辑核对与本地 mock worker 回归验证; P0 已完成; P1.1 已完成旧逻辑核对与本地 latest.json/installer mock 回归验证; P1.2 已完成旧逻辑核对与本地生命周期回归验证; P1.3 已完成旧逻辑核对与本地回归验证; P1.4 已完成旧逻辑核对与本地回归验证; P1 已完成; P2.1 已完成旧逻辑核对与本地回归验证; P2.2 已完成旧逻辑核对与本地回归验证; P2.3 已完成旧逻辑核对与本地回归验证; P2.4 已完成旧逻辑核对与本地回归验证; P2 已完成; P3.1 已完成文档口径核对与修正; P3.2 已完成接口契约说明; P3 已完成
> 范围: 只记录 v1.0.3 已有、当前版本缺失或未完整迁移的内容; 不修改 `litellm/` 参考目录。

## 目标

把当前 Rust/Tauri 版本相对 v1.0.3 漏掉的功能按重要等级拆成可推进任务。后续实现时按本文档任务编号推进, 每完成一项都需要补充执行记录和验证结果, 避免再次出现前端入口保留但后端实际为空实现的情况。

## 执行约束

- P0/P1 缺口默认按 `v1.0.x` 旧版逻辑直接翻译到当前 Rust/Tauri 架构, 不自行构思新逻辑替代旧逻辑。
- 后续 P0/P1 实现必须先完整对照并转化 `v1.0.x` 旧代码; 除测试、错误透出、当前架构必要适配外, 不新增旧版不存在的产品逻辑。
- 如果旧逻辑依赖的运行条件在当前架构中不存在, 必须先记录差异和迁移边界, 再做等价翻译; 不能用新的产品语义绕开。
- P2/P3 后续按小节提交: 同一大节复用同一个 PR; 每完成一小节提交并更新 PR 后, 主动评论 `@copilot` 请求审查。
- 小节提交并更新 PR 后不等待 Copilot 审查和 CI 测试, 直接开始下一小节; 下一小节完成并准备提交前, 先处理完上一小节的审查与 CI 结果, 再提交并继续。

## 优先级定义

- `P0`: 用户可见且会造成误导、假成功、点击异常或数据风险的缺口。优先处理。
- `P1`: v1.0.3 已有的核心产品能力, 当前缺失会明显降低可用性, 但通常不会立即破坏主链路。
- `P2`: 协议兼容、边界场景和长期正确性问题。影响特定请求形态或高级用法。
- `P3`: 文档、口径和清理项。需要跟随实现同步修正, 防止交接和发布说明误导。

## 当前已补回的内容

这些内容不列入缺口任务, 但后续发布前仍要用测试和实机验证确认。

- Codex 0.128+ 的 `model_catalog_json` 方向已经有当前推进文档: `docs/codex-0.128-model-catalog-compat-plan.md`。
- 当前 `desktop_configure` 已把 provider 名称、默认模型、模型映射和能力传入 `apply_provider`: `src-tauri/src/admin/handlers.rs:712-745`。
- 当前 proxy resolver 已支持把 Codex 侧 GPT 槽位映射到 provider 真实模型, 并清理内部模型后缀: `crates/proxy/src/resolver.rs:128-143`。

## P0: 先消除假成功和直接可见故障

### P0.0 统一处理所有 stub 接口的用户可见行为

**问题**: 当前路由注释声称 `/api/*` shape 严格对齐 v1.4, 但多个接口仍是 stub。用户看到的是操作成功, 实际没有完成动作。

**证据**:

- 当前路由注释: `src-tauri/src/admin/mod.rs:1-10`
- 当前 release note 自相矛盾: `docs/release-notes-v2.0.0.md:3` 声称功能完全兑现, `docs/release-notes-v2.0.0.md:101-105` 又列出多个 stub

**推进要求**:

- 每个 stub 必须优先按 `v1.0.x` 旧版实现直接翻译, 保持旧接口返回结构和错误语义。
- 禁止保留“返回 success 但不实际工作”的行为。
- 只有确认旧逻辑在当前架构没有等价运行条件时, 才允许记录阻塞并暂缓, 不能自行设计新语义替代。

**验收**:

- 前端所有可点击入口不会因为缺少字段抛异常。
- 未实现功能不会显示为成功完成。

**当前状态**: 已完成。已移除 P0 范围内的假成功 stub, 改为按 `v1.0.3` 旧实现转译 provider 测试、模型探测、用量查询、兼容性矩阵、配置备份/导入、代理日志/统计和反馈提交。

### P0.1 恢复模型探测和自动填充

**问题**: v1.0.3 会真实请求 provider 模型列表并自动生成映射; 当前版本只返回空列表或 success。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:930-939`
- 当前前端仍调用接口: `frontend/js/api.js:194-203`
- 当前前端点击“获取模型”后读取 `result.models.length`, 但后端 `autofill` 没有返回 `models`: `frontend/js/app.js:1708-1718`
- v1.0.3 对照: `v1.0.3:backend/provider_tools.py`, `v1.0.3:backend/main.py`

**推进要求**:

- 迁移 v1.0.3 的 model endpoint 候选、模型 ID 提取、默认映射建议逻辑。
- `autofill` 返回前端需要的 `models` 字段, 并保存建议映射。
- 对 API key、base URL、网络错误给出明确错误信息。

**验收**:

- 点击“获取模型”不会报错。
- 至少用一个 OpenAI-compatible provider 验证能拉取模型并填充默认模型映射。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 实现已按 `v1.0.3:backend/provider_tools.py` 迁移模型 endpoint 候选、模型 ID 提取、非对话模型过滤和 `default` 推荐映射; `autofill` 会返回 `models` 并保存 `suggested`。本次补充前端失败态, 模型管理页和 provider 表单页获取模型失败时都会把后端错误原因写入页面结果文本, 不再停留在“正在获取模型列表”。真实外网 provider 验证仍需具备可用 API key 和网络条件后补做。

### P0.2 恢复 provider 测试、用量查询和兼容性矩阵

**问题**: 当前测试接口直接成功, 用量为空, 兼容性为空。用户无法判断 provider 是否可用。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:914-927`
- 当前前端仍暴露调用: `frontend/js/api.js:174-187`, `frontend/js/app.js:1801-1806`
- v1.0.3 对照: `v1.0.3:backend/main.py`, `v1.0.3:backend/provider_tools.py`

**推进要求**:

- provider test 必须真实发起最小请求或等价健康检查。
- usage 查询按 v1.0.3 支持范围迁移, 对不支持的 provider 明确返回 unsupported。
- compatibility 返回真实 provider 兼容信息, 不再返回空数组伪装成功。

**验收**:

- 错误 key、错误 base URL、正常 provider 三类路径有可区分结果。
- 兼容性检查页面显示真实结果或明确说明不支持。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 实现已按 `v1.0.3:backend/main.py` 迁移 provider 最小连接测试、Kimi 认证失败提示和兼容性矩阵; 已按 `v1.0.3:backend/provider_tools.py` 迁移 DeepSeek、SiliconFlow、OpenRouter、Novita、StepFun 的余额 endpoint 识别和响应归一化。补充修正 `query_provider_usage` HTTP 语义, 让未保存 API Key 时保持旧版的 JSON 返回而不是额外包装成 HTTP 400。真实外网 provider 的余额接口和真实认证失败路径仍需具备可用 API key 和网络条件后补做。

### P0.3 恢复配置备份、备份列表和导入

**问题**: 当前备份返回 `stub`, 列表为空, 导入接口返回 success 但不导入。这个缺口会导致用户误以为配置已经备份或恢复。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:869-889`
- 当前前端仍暴露调用: `frontend/js/api.js:292-306`, `frontend/js/app.js:1870-1875`
- v1.0.3 对照: `v1.0.3:backend/main.py`

**推进要求**:

- 备份真实写入本应用配置目录。
- 备份列表读取真实文件。
- 导入需要校验格式、写入 registry, 并避免破坏现有 secrets。

**验收**:

- 创建备份后刷新列表能看到真实文件。
- 导出后再导入能恢复 provider、settings、active provider 等关键字段。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 实现已按 `v1.0.3:backend/config.py` / `v1.0.3:backend/main.py` 迁移真实备份文件写入、备份列表、导出格式、导入前自动备份、导入 normalize 和 registry 写入。为满足当前任务的 secret 保护边界, 本次补充当前架构必要适配: 导入文件中同 ID provider 缺少或留空 `apiKey` / 敏感 `extraHeaders` 时, 保留本机现有值, 避免不完整导入误清空本机 secret。

### P0.4 恢复代理日志、统计和打开日志目录

**问题**: 当前 proxy status 的统计固定为 0, logs 固定为空, clear/open-dir 固定成功。用户无法排查代理请求。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:810-836`
- 当前前端仍读取日志和统计: `frontend/js/api.js:251-270`, `frontend/js/api.js:320-325`
- v1.0.3 对照: `v1.0.3:backend/main.py`

**推进要求**:

- proxy 转发链路记录请求结果、状态、耗时和错误摘要。
- dashboard/proxy 页面统计来自真实日志或内存计数。
- clear logs 和 open log dir 执行真实动作; 不支持的平台返回明确错误。

**验收**:

- 代理成功请求和失败请求都能在日志中看到。
- 清空日志后页面同步为空。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 实现已按 `v1.0.3:backend/proxy.py` 迁移 `ProxyStats`、`LogBuffer`、按天文件日志和清空归档逻辑; `forward_handler` 已在成功响应、解析/鉴权失败和上游失败路径记录统计与日志; `/api/proxy/status`、`/api/proxy/logs`、`/api/proxy/logs/clear` 和 `/api/proxy/logs/open-dir` 已从真实 telemetry 和日志目录执行。补充回归测试覆盖统计、内存日志、磁盘日志、清空归档、成功转发日志和网关认证失败日志。打开日志目录会启动系统文件管理器, 自动化测试不直接拉起 GUI, 仍需实机 UI 点击确认。

### P0.5 恢复反馈提交

**问题**: 当前反馈接口直接返回成功, 没有发送到 worker, 也没有 v1.0.3 的校验、限流、附件和诊断信息处理。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:908-910`
- 当前前端仍提交反馈: `frontend/js/api.js:313-317`
- v1.0.3 对照: `v1.0.3:backend/main.py`

**推进要求**:

- 恢复字段校验、大小限制、附件处理和诊断信息收集。
- worker 地址缺失时返回明确配置错误, 不回传 secret。
- 失败时前端显示失败原因。

**验收**:

- 正常反馈能到达 worker。
- 缺 worker 配置、附件过大、网络失败都有清晰错误。

**当前状态**: 已完成代码核对和本地 mock worker 回归验证。当前 Rust 实现已按 `v1.0.3:backend/main.py` 迁移反馈字段校验、非 JSON 请求错误、成功/失败限流、base64 附件大小过滤、multipart worker 转发、诊断 meta、最近 200 行代理日志附加、worker 错误透出和成功返回 `id` / `email_sent`。本次补齐当前架构必要适配: worker URL 为空时返回明确配置错误且不回传 secret; 代理日志读取改为 lossless/替换式解码, 与旧版 `errors="replace"` 行为一致。真实 Cloudflare Worker 收件结果仍需具备外网条件后补做。

## P1: 恢复核心产品能力

### P1.1 恢复更新检查和安装

**问题**: 当前检查更新固定返回无更新, 安装接口返回未实现。前端更新按钮还在。

**证据**:

- 当前后端空实现: `src-tauri/src/admin/handlers.rs:893-903`
- 当前前端仍调用更新流程: `frontend/js/api.js:282-289`, `frontend/js/app.js:1812-1865`
- v1.0.3 对照: `v1.0.3:backend/update.py`

**推进要求**:

- 恢复 latest metadata 拉取、版本比较、平台资产选择和下载。
- 安装流程按 macOS / Windows / Linux 分平台处理。
- 如果暂不恢复自动安装, 前端必须隐藏安装入口或明确只支持手动下载。

**验收**:

- 本地旧版本对照 metadata 能识别更新。
- 无可用资产、网络失败、已是最新版三类路径有明确结果。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 实现已按 `v1.0.3:backend/update.py` / `v1.0.3:backend/main.py` 迁移 latest metadata 拉取、UTF-8 BOM 容错、版本比较、平台 key 识别、平台资产校验、Windows/macOS 安装资产选择、安装包下载、临时文件替换、sha256 校验、已是最新版返回和安装器启动语义。当前 Tauri 架构没有旧版 `_update_quit_handler`, 因此 macOS 安装流程按旧版无 quit handler 路径打开安装包并返回 `quitRequested: false`; 旧版延迟退出命令保留为测试锁定的迁移边界。

### P1.2 恢复启动和退出生命周期

**问题**: v1.0.3 支持启动自动应用 active provider、退出恢复 Codex 配置、托盘快速切换 provider。当前 Tauri 主流程只处理窗口显示、隐藏、退出和停止代理。

**证据**:

- 当前托盘只有显示、隐藏、退出: `src-tauri/src/main.rs:44-78`, `src-tauri/src/main.rs:145-155`
- 当前退出只停止代理: `src-tauri/src/main.rs:103-108`
- v1.0.3 对照: `v1.0.3:backend/main.py`

**推进要求**:

- 根据 settings 恢复启动自动 apply / auto start proxy 行为。
- 根据 settings 恢复退出 restore Codex 行为。
- 托盘 provider 快速切换如暂不恢复, 需要在 UI 和文档中明确移除。

**验收**:

- 开启对应设置后重启应用, Codex config 和 proxy 状态符合预期。
- 退出应用后 Codex config 按设置恢复或保留。

**当前状态**: 已完成代码核对和本地回归验证。当前 Tauri 主流程已按 `v1.0.3:main.py` / `v1.0.3:backend/main.py` 迁移启动残留快照恢复、`autoApplyOnStart` 自动应用 active provider、按需启动本地代理、退出停代理、`restoreCodexOnExit` 还原 Codex 配置、provider 切换时同步 Codex 配置和托盘 provider 快速切换菜单。当前 Rust 代理使用启动时静态 resolver, 因此 provider 切换同步会重启代理以加载最新配置, 这是旧版动态读取配置逻辑在当前架构下的必要适配。

### P1.3 恢复 `/api/version`

**问题**: v1.0.3 有版本接口, 当前 router 没有注册 `/api/version`。

**证据**:

- 当前路由表缺少 `/api/version`: `src-tauri/src/admin/mod.rs:24-115`
- 当前版本来源: `src-tauri/Cargo.toml:1-4`
- v1.0.3 对照: `v1.0.3:backend/main.py`

**推进要求**:

- 新增 `/api/version`, 返回当前 app version 和必要平台信息。
- 前端如不再使用该接口, 也要确认外部脚本或旧文档没有依赖。

**验收**:

- `/api/version` 返回 `2.0.2` 或构建时注入的真实版本。

**当前状态**: 已完成代码核对和本地回归验证。当前 Rust 路由已按 `v1.0.3:backend/main.py` 恢复 `/api/version`, 返回结构保持旧版 `{ "version": APP_VERSION }`, 版本值来自当前 Cargo package version。

### P1.4 修正 Desktop status 的旧 Claude 模型槽位

**问题**: 当前 Desktop status 仍硬编码 `sonnet`、`haiku`、`opus`, 与当前 GPT 槽位迁移不一致。

**证据**:

- 当前后端硬编码旧槽位: `src-tauri/src/admin/handlers.rs:669-679`
- 当前前端 fallback 也保留旧槽位: `frontend/js/api.js:206-220`

**推进要求**:

- Desktop status 使用当前 Codex/GPT 槽位或直接从 active provider/model catalog 派生。
- `health.issues` 不应永远为空; 至少反映未 apply、snapshot 缺失、catalog 不一致等状态。

**验收**:

- Desktop 页面展示的模型与实际 apply 到 Codex 的模型目录一致。

**当前状态**: 已完成。当前 Rust 状态接口已按 `v1.0.3:backend/main.py` 的 `_desktop_health` 逻辑恢复 base URL mismatch、非本工具管理配置和 1M 模型目录未写入检查; Desktop status 不再硬编码 `sonnet` / `haiku` / `opus`, 改为从 active provider 通过当前 Codex/GPT model catalog 生成 `inferenceModels` 展示值。前端 fallback 已改为空数组, 避免在后端缺失字段时重新显示旧 Claude 槽位。

## P2: 协议兼容和边界覆盖

### P2.1 补回 `previous_response_id` 和 session cache

**问题**: 当前 Responses 请求转换是 stateless, 明确丢弃 `previous_response_id`。

**证据**:

- 当前注释: `crates/adapters/src/responses/request.rs:1-25`

**推进要求**:

- 设计会话缓存边界, 明确存储位置、生命周期和清理策略。
- 恢复多轮历史时保持工具调用、reasoning 和 function output 的顺序正确。

**验收**:

- 带 `previous_response_id` 的续轮请求能恢复上一轮必要上下文。

**当前状态**: 已完成代码核对和本地回归验证。`v1.0.3` tag 中 `backend/proxy.py` 已调用 `session_cache`, 但 tag 本身缺少对应模块; 已从旧提交 `9c9462f` / `904095d` 取回旧版 `ResponseSessionCache` 与 Responses 续轮拼接逻辑并直接转译到当前 Rust adapter 架构。当前实现恢复 `previous_response_id` 命中缓存时的历史消息前置、重复 instructions 去重、工具调用和 tool output 顺序保留, 并在 Chat SSE 转 Responses SSE 完成后把本轮请求消息和 assistant 输出写回 session cache。

### P2.2 补齐 Responses 请求字段覆盖

**问题**: 当前仍未处理图像、文件、音频、视频、`text.format`、`store`、`metadata`、`prediction`、`service_tier` 等字段。

**证据**:

- 当前注释: `crates/adapters/src/responses/request.rs:16-25`

**推进要求**:

- 先按 Codex CLI 实际会发出的字段排序。
- 对 provider 不支持的字段明确丢弃、降级或报错, 不要静默改变语义。

**验收**:

- 每个字段都有单测覆盖: 转发、降级、拒绝三类行为必须明确。

**当前状态**: 已完成代码核对和本地回归验证。已按旧提交 `904095d:backend/responses_adapter.py` / `backend/base_adapter.py` / `backend/openai_adapter.py` 迁移 Responses 请求字段覆盖: 顶层 `text.format` 转 `response_format`、`reasoning` 转 `reasoning_effort`、`store` / `metadata` / `prediction` / `service_tier` / `modalities` / `audio` 校验清洗、扩展透传字段、`tool_choice` 规范化、多模态 `input_image` / `input_file` / `input_audio` / `input_video` item 降级、未知 content block 文本降级、连续 user/assistant message 合并, 以及非 OpenAI 官方 provider 的 `developer` role 降级为 `system`。

### P2.3 补齐 Chat SSE 转 Responses SSE 的 legacy 和多 choice 支持

**问题**: 当前流转换不处理 legacy `delta.function_call`, 且只看 `choices[0]`。

**证据**:

- 当前注释: `crates/adapters/src/responses/converter.rs:13-17`

**推进要求**:

- 增加 legacy function_call fixture。
- 明确多 choice 的合并策略或拒绝策略。

**验收**:

- legacy function_call 不再静默丢失。
- 多 choice 行为有文档和测试。

**当前状态**: 已完成代码核对和本地回归验证。已对照旧提交 `904095d:backend/streaming_adapter.py` 与 `904095d:backend/chat_responses_adapter.py` 迁移旧版行为: legacy `delta.function_call` 现在转为 index=0 的 Responses `function_call` item 并复用现有 tool call 生命周期; `finish_reason=function_call` 映射为 completed; 多 choice 保留 1.0.x 只读取 `choices[0]` 的策略, 不自行合并或展开其他候选, 并用单测锁定。

### P2.4 补完整 provider slug 和 model alias 算法

**问题**: v1.0.3 支持基于 provider id/name 的 slug 归一化和 model alias。当前 resolver 只支持 `slug == provider.id`。

**证据**:

- 当前注释: `crates/proxy/src/resolver.rs:112-117`
- v1.0.3 对照: `v1.0.3:backend/model_alias.py`

**推进要求**:

- 迁移 provider slug 归一化规则。
- 保持当前 GPT 槽位到 provider 真实模型的映射优先级。
- 避免恢复旧 Claude 命名作为主路径。

**验收**:

- provider id、provider name、带特殊字符 name 三类 slug 均可路由到正确 provider。

**当前状态**: 已完成代码核对和本地回归验证。已对照 `v1.0.3:backend/model_alias.py` 迁移 provider slug 归一化规则: provider id 优先、空 id 回退 provider name、小写后把非 `[a-z0-9_-]` 连续字符折叠为 `-`、去掉首尾 `-/_` 并限制 56 字符; 当前 resolver 的 `<slug>/<model>` 路由改为复用该规则。OpenAI GPT 槽位解析补回旧版 trim/lower 行为, 同时保留当前槽位模型优先、default 降级、剥离内部 `[1m]` 后缀的映射优先级。

## P3: 文档和发布口径修正

### P3.1 修正 v2.0.0 release note 的功能兑现表述

**问题**: 当前文档同时写“功能完全兑现”和“多个路由 stub”, 会误导发布和后续排查。

**证据**:

- `docs/release-notes-v2.0.0.md:3`
- `docs/release-notes-v2.0.0.md:101-105`

**推进要求**:

- 在缺口实现前, 文档必须明确哪些功能是已实现、哪些是暂未接通。
- 每个恢复任务完成后, 再更新对应 release note 或后续版本说明。

**验收**:

- README、release note、实际代码行为三者不互相矛盾。

**当前状态**: 已完成文档口径核对与修正。`docs/release-notes-v2.0.0.md` 已取消"功能与 v1.0.4 完全兑现"的过度表述, 改为说明 v2.0.0 首发只完成核心迁移主路径, 当时仍有管理路由和兼容边界 stub; 已知限制改为历史状态并指向 README / 本推进文档作为当前主线状态。`README.md` 已同步当前版本源 `2.0.2`、v2.0.1 起三平台 release 资产、以及当前 GitHub Actions + Tauri bundler 的正式发布链路。

### P3.2 整理接口契约说明

**问题**: 当前前端沿用 v1 入口, 后端有 Rust route, 但缺少一份明确的接口状态表。

**推进要求**:

- 建立 `/api/*` 路由状态清单: 已实现、stub、计划移除、前端是否仍调用。
- 对每个 stub 标出关联任务编号。

**验收**:

- 后续排查可以直接从接口状态表定位剩余缺口。

**当前状态**: 已完成接口契约说明。新增 `docs/api-route-status.md`, 以当前 `src-tauri/src/admin/mod.rs`、`src-tauri/src/admin/handlers.rs`、`frontend/js/api.js`、`crates/proxy/src/server.rs` 和 adapter 代码为准建立 `/api/*` 路由状态表, 标出已实现、conditional、compatibility、not registered 和 frontend caller。当前审计未发现仍注册的 `/api/*` stub; 同时把非 `/api/*` 的 proxy surface 单独列出, 明确当前 Rust 主线只承诺 HTTP/SSE 转发, 未注册 v1.0.3 的 WebSocket endpoint。为避免文档任务触发不必要的 Tauri CI, 最终 diff 保持在 README / docs / 推进文档范围, 不修改 Rust 源码注释。

## 建议推进顺序

1. `P0.0` 先处理所有假成功行为, 按 `v1.0.x` 旧逻辑直接翻译成真实实现。
2. `P0.1` 到 `P0.5` 按用户可见风险逐项恢复真实能力。
3. `P1.1` 到 `P1.4` 恢复 v1.0.3 的核心产品体验。
4. `P2.1` 到 `P2.4` 补协议兼容和边界场景。
5. `P3.1` 和 `P3.2` 跟随每批实现同步更新, 最后统一校对发布口径。

## 执行记录

- 2026-05-05: 创建本文档。依据当前源码、`v1.0.3` 对照排查结果、`docs/release-notes-v2.0.0.md` 和 `docs/codex-0.128-model-catalog-compat-plan.md` 整理任务树。
- 2026-05-05: 根据用户补充约束, 明确 P0/P1 缺口推进必须直接翻译 `v1.0.x` 旧有逻辑, 不自行构思替代逻辑。
- 2026-05-05: 推进 `P0.0`, 按 `v1.0.3:backend/provider_tools.py` 直接翻译模型列表 endpoint 候选、模型 ID 提取、推荐映射、余额/用量查询和兼容性矩阵逻辑; 按 `v1.0.3:backend/config.py` 直接翻译配置备份、备份列表、导出格式和导入前备份逻辑。
- 2026-05-05: 继续推进 `P0.0`, 按 `v1.0.3:backend/proxy.py` 直接翻译 `ProxyStats`、`LogBuffer`、按天日志文件、清空日志时归档和代理转发日志记录; 按 `v1.0.3:backend/main.py` 直接翻译代理日志列表、日志目录打开、反馈字段校验、进程内节流、附件 multipart 转发、诊断信息和最近 200 行代理日志附加逻辑。
- 2026-05-05: 推进 `P0.1`, 对照 `v1.0.3:backend/provider_tools.py` 和当前 Rust 实现确认模型探测主逻辑已完成等价迁移; 新增本地 OpenAI-compatible `/v1/models` mock 回归测试, 覆盖模型列表拉取、embedding 过滤后的默认模型推荐和 endpoint 返回; 修正前端获取模型失败态, 让 API key、base URL 或网络错误原因显示在页面结果文本中。
- 2026-05-05: 推进 `P0.2`, 对照 `v1.0.3:backend/main.py` 和 `v1.0.3:backend/provider_tools.py` 核对 provider 测试、余额/用量查询和兼容性矩阵逻辑; 将 `query_provider_usage` 的未保存 API Key 返回语义改回旧版 JSON 语义; 新增本地 mock 回归测试覆盖正常 provider 最小 POST、错误 API Key、错误 API 地址、未支持 provider、余额字段归一化和兼容性矩阵。
- 2026-05-05: 推进 `P0.3`, 对照 `v1.0.3:backend/config.py` / `v1.0.3:backend/main.py` 确认备份、列表、导出、导入主逻辑已完成等价迁移; 新增隔离 HOME 的本地回归测试覆盖真实备份文件写入、备份列表、导入前自动备份、导入写入 registry, 并补充导入缺失 secret 时保留本机 `apiKey` / `extraHeaders` 的当前架构适配。
- 2026-05-05: 开始推进 `P0.4`, 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-05: 完成 `P0.4`, 对照 `v1.0.3:backend/proxy.py` 和 `v1.0.3:backend/main.py` 确认当前 Rust 代理统计、内存日志、按天文件日志、清空归档、管理端读取日志/统计和打开日志目录行为已按旧逻辑迁移; 补充 telemetry 与端到端代理回归测试。
- 2026-05-06: 开始推进 `P0.5`, 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P0.5`, 对照 `v1.0.3:backend/main.py` 确认当前 Rust 反馈提交已经转为真实 worker multipart 转发; 补齐非 JSON 请求旧版错误、代理日志非 UTF-8 容错、worker URL 空值错误和本地 mock worker 回归测试, 覆盖成功提交、上游错误、字段校验、限流、附件过滤和最近 200 行日志。
- 2026-05-06: 处理 PR #20 远端 CI 阻塞。Tauri job 失败发生在 GitHub runner 安装 `libwebkit2gtk-4.1-dev` 等系统依赖超时, 未进入 `cargo check` / `cargo test`; 将该步骤超时从 8 分钟放宽到 20 分钟, 让既有 retry 和依赖安装有足够时间完成。
- 2026-05-06: PR #20 通过 Copilot 审查和全部 CI 后 squash 合并到 `main`; 从最新 `origin/main` 创建 `codex/p1-core`, 开始推进 `P1.1` 更新检查和安装。
- 2026-05-06: 完成 `P1.1`, 对照 `v1.0.3:backend/update.py` 和 `v1.0.3:backend/main.py` 将 `/api/update/check` / `/api/update/install` 从固定无更新 stub 恢复为真实 latest.json 检查、平台资产选择、安装包下载和安装器启动; 补充本地 latest.json/installer mock 回归测试覆盖更新识别、安装包 sha256 校验、路径名净化、错误 sha 和 Linux 不支持安装路径。
- 2026-05-06: 开始推进 `P1.2`, 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P1.2`, 对照 `v1.0.3:main.py` 和 `v1.0.3:backend/main.py` 恢复启动自动恢复/自动应用、退出恢复、active provider 切换同步和托盘 provider 快速切换; 因当前 Rust 代理 resolver 是启动快照, provider 切换时会重启代理以等价获得旧版按请求读取最新配置的效果。
- 2026-05-06: 提交 `P1.2` 前处理 `P1.1` 审查结果。Copilot 未提出阻塞问题; 但其将 `update_install` 空 body 行为判定为无需改动。经对照旧版 `v1.0.3:backend/main.py`, 旧逻辑支持非 JSON 请求体按空对象处理, 因此补齐当前 Rust `update_install` 的空 body 兼容。
- 2026-05-06: 开始推进 `P1.3`, 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P1.3`, 对照 `v1.0.3:backend/main.py` 恢复 `/api/version` 路由和旧版响应结构 `{ "version": APP_VERSION }`。
- 2026-05-06: 开始推进 `P1.4`, 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P1.4`, 对照 `v1.0.3:backend/main.py` 的 `_desktop_health` 与 `v1.0.3:backend/registry.py` 的 `provider_inference_models` 迁移思路, 将当前状态接口的模型展示改为由 active provider 的 Codex/GPT model catalog 派生; 同时恢复 base URL mismatch、非本工具管理配置和 1M catalog 未写入健康问题, 并移除前端旧 Claude 模型槽位 fallback。
- 2026-05-06: 提交 `P1.4` 前处理 `P1.3` 审查和 CI 结果。Copilot 未提出阻塞问题; 远端 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过。
- 2026-05-06: PR #21 通过 Copilot 审查和全部 CI 后 squash 合并到 `main`; 从最新 `origin/main` 创建 `codex/p2-protocol`, 开始推进 `P2.1` previous_response_id 和 session cache。
- 2026-05-06: 完成 `P2.1`, 对照旧提交 `9c9462f` / `904095d` 中的 `backend/session_cache.py`、`backend/responses_adapter.py` 和 `backend/proxy.py` 迁移 previous_response_id 续轮缓存逻辑; 新增 Rust 侧 `ResponseSessionCache`, 在 Responses 请求转换时恢复历史消息, 在流式响应完成后缓存本轮请求与 assistant 输出, 并把请求计划传入响应流转换以复用同一个 response id。
- 2026-05-06: 创建 P2 共用 PR #22 并提交 `P2.1` 后, 按用户更新后的节奏不等待 Copilot 审查和 CI 完成, 直接开始推进 `P2.2`; 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P2.2`, 对照旧提交 `904095d` 中的 Responses 请求字段转换逻辑, 补齐当前 Rust adapter 的多模态 input item、结构化输出、reasoning effort、特殊参数清洗、扩展字段透传、tool_choice 规范化、连续消息合并和 developer role 兼容处理; 新增单测覆盖转发、降级和拒绝/丢弃三类行为。
- 2026-05-06: 提交 `P2.2` 前处理 `P2.1` 审查和 CI 结果。PR #22 远端 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过; Copilot 提出 previous_response_id 命中时本轮 instructions 会被已有 system/developer history 覆盖, 经对照旧版 `backend/responses_adapter.py::_build_messages_from_input` 确认为旧逻辑原样迁移, 本轮不改为新语义, 已在 PR 中回复说明。
- 2026-05-06: 提交 `P2.2` 并更新 PR #22 后, 按用户更新后的节奏不等待 Copilot 审查和 CI 完成, 直接开始推进 `P2.3`; 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P2.3`, 对照旧提交 `904095d` 中的 Chat SSE 到 Responses SSE 转换逻辑, 补齐 legacy `delta.function_call` 到 Responses `function_call` item 的流式生命周期, 并将 `finish_reason=function_call` 恢复为 completed; 多 choice 保留旧版 `choices[0]` 兼容策略, 不新增旧版没有的合并语义。
- 2026-05-06: 提交 `P2.3` 前处理 `P2.2` 审查和 CI 结果。PR #22 远端 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过; review 和 review comment 列表未出现新的 `P2.2` 代码审查意见, 仅保留前序已回复的 `P2.1` 旧逻辑说明。
- 2026-05-06: 提交 `P2.3` 并更新 PR #22 后, 评论 `@copilot` 请求审查; 按用户更新后的节奏不等待 Copilot 审查和 CI 完成, 直接开始推进 `P2.4`; 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P2.4`, 对照 `v1.0.3:backend/model_alias.py` 迁移 provider slug 归一化和 OpenAI 槽位大小写兼容逻辑; 当前 resolver 现在可用 provider id、空 id 时的 provider name、带特殊字符的 provider name 生成的 slug 路由到对应 provider, 并继续保持 GPT 槽位映射的 specific slot 优先、default 降级和 `[1m]` 后缀清理。
- 2026-05-06: 提交 `P2.4` 前处理 `P2.3` 审查和 CI 状态。PR #22 远端 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过; review 和 review comment 列表未出现新的 `P2.3` 代码审查意见。`@copilot` 触发的 `copilot` check 等待多轮后仍为 in_progress, 尚无可处理结果, 本次不因无结果检查无限阻塞提交。
- 2026-05-06: PR #22 通过常规 CI 后 squash 合并到 `main`, 合并提交 `f57a96c6`; 该 PR 覆盖 `P2.1` 到 `P2.4` 的协议兼容和边界场景恢复。合并前唯一未解决 review thread 为 `P2.1` 已按旧逻辑回复过的 instructions 行为, 已确认不改新语义并标记 resolved。
- 2026-05-06: 从最新 `origin/main` 创建 `codex/p3-docs`, 开始推进 `P3.1`; 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P3.1`, 核对 `docs/release-notes-v2.0.0.md`、`README.md`、`src-tauri/Cargo.toml` 和 `docs/build.md` 后修正文档口径: v2.0.0 release note 不再声称 v1.0.4 功能完全兑现, stub 列表明确为首发历史状态; README 同步当前版本 `2.0.1`、三平台 release 资产和正式发布链路。
- 2026-05-06: 创建 P3 共用 PR #23 并提交 `P3.1` 后, 评论 `@copilot` 请求审查; 按用户更新后的节奏不等待 Copilot 审查和 CI 完成, 直接开始推进 `P3.2`; 已先将任务状态标记为正在处理, 避免与其他 worktree 并行认领同一项。
- 2026-05-06: 完成 `P3.2`, 扫描当前 Rust admin route、frontend 调用入口、proxy fallback 和 adapter 注册表后新增 `docs/api-route-status.md`; 确认当前 `/api/*` 注册路由无已知 stub, 并把 v1.0.3-only 的 `GET /api/providers/{id}/models`、当前 proxy HTTP/SSE surface、WebSocket endpoint 未注册、`/v1/models` 不再由本地 gateway 合成等状态写入契约表。同步修正 README 中仍沿用旧版 WebSocket 传输承诺的过期表述。
- 2026-05-06: 提交 `P3.2` 前处理 `P3.1` 审查和 CI 结果。PR #23 当前 head `95944aa` 的 `Detect changed paths` 和 `Rust · fast workspace check (no Tauri sysdeps)` 通过, `Rust · Tauri (src-tauri) check` 因 docs-only 变更被跳过; reviews 和 review comments 为空, 暂无 Copilot 输出需要处理。
- 2026-05-06: `P3.2` 首次推送后发现 comment-only Rust 源码文档改动会让 PR diff 命中 Rust/Tauri 检查路径; 为遵守“不涉及 Tauri 就不跑对应 CI”的策略, 撤回 Rust 源码注释改动, 将接口状态和过期口径说明仅保留在 README、`docs/api-route-status.md` 和推进文档中。
- 2026-05-06: 在 P0 到 P3 缺口全部完成并合并后, 将当前主线版本推进到 `2.0.2`, 新增 `docs/release-notes-v2.0.2.md` 汇总 v1.0.x 旧逻辑迁移补齐内容, 并准备通过非正式 `v2.0.2-rc1` tag 触发三平台打包工作流; 本轮只做草稿打包验证, 不发布正式 Release。

## 验证记录

- 2026-05-05: 未运行构建或测试。当前文档只记录静态排查结果和后续推进顺序。
- 2026-05-05: `cargo fmt` 通过。
- 2026-05-05: `cargo check` 通过。
- 2026-05-05: `cargo test -p codex-app-transfer provider_test` 通过, 3 个 provider test helper 相关单测通过。
- 2026-05-05: `cargo test -p codex-app-transfer-proxy --lib` 通过, 16 个代理 resolver/forward 单测通过。
- 2026-05-05: `cargo test -p codex-app-transfer-proxy` 中库单测通过, 但 `tests/auth_and_routing.rs` 7 个集成测试因当前沙箱不允许 `TcpListener::bind("127.0.0.1:0")` 失败, 失败点为测试夹具本地端口监听权限, 不是业务断言失败。
- 2026-05-05: 反馈提交未做真实 worker 联通验证; 当前环境网络受限, 仅完成编译与本地静态路径验证。
- 2026-05-05: `cargo test -p codex-app-transfer fetch_provider_models_reads_openai_compatible_models` 通过, 本地 mock `/v1/models` 返回 OpenAI-compatible 模型列表后可提取 3 个模型, 并推荐 `deepseek-v4-pro` 作为 `default`。
- 2026-05-05: `cargo test -p codex-app-transfer` 通过, 4 个 Tauri admin handler 单测通过。
- 2026-05-05: `node --check frontend/js/app.js` 通过。
- 2026-05-05: `cargo test -p codex-app-transfer provider_` 在正常本机权限下通过, 8 个 provider 相关单测覆盖旧版 URL 拼接、模型选择、模型探测、连接测试、错误 API Key、未支持余额查询和兼容性矩阵。
- 2026-05-05: `cargo test -p codex-app-transfer balance_payloads_match_legacy_normalization` 通过, 覆盖 DeepSeek、OpenRouter 和通用余额响应归一化。
- 2026-05-05: `cargo test -p codex-app-transfer` 在正常本机权限下通过, 9 个 Tauri admin handler 单测通过。
- 2026-05-05: `node --check frontend/js/app.js` 通过。
- 2026-05-05: `cargo test -p codex-app-transfer-proxy telemetry` 通过, 3 个 telemetry 单测覆盖统计、内存日志、按天文件日志和清空归档。
- 2026-05-05: `cargo test -p codex-app-transfer-proxy proxy_telemetry` 在正常本机权限下通过, 2 个端到端测试覆盖成功转发与网关认证失败的统计和日志记录。
- 2026-05-05: `cargo test -p codex-app-transfer-proxy` 在正常本机权限下通过, 19 个库单测、9 个 auth/routing 集成测试和 2 个 streaming passthrough 测试全部通过。
- 2026-05-05: `cargo test -p codex-app-transfer config_` 通过, 2 个 P0.3 配置备份/导入相关单测通过。
- 2026-05-05: 合并远端 P0.2 后重新验证: `cargo fmt --check` 通过, `git diff --check` 通过, `cargo test -p codex-app-transfer` 在提升权限环境通过, 11 个 Tauri admin handler 单测全部通过。提升权限原因是本地 mock 测试需要监听 `127.0.0.1:0`, 普通沙箱会拒绝端口绑定。
- 2026-05-05: 同步最新 `origin/main` 的 P0.4 认领标记后重新验证: `cargo fmt --check` 通过, `git diff --cached --check` 通过, `cargo test -p codex-app-transfer` 在提升权限环境通过, 11 个 Tauri admin handler 单测全部通过。
- 2026-05-05: rebase 到已完成 P0.4 的最新 `origin/main` 后重新验证: `cargo fmt --check` 通过, `git diff --check origin/main...HEAD` 通过, `cargo test -p codex-app-transfer` 在提升权限环境通过, 11 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo fmt` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer feedback` 在普通沙箱下因本地 mock worker 需要监听 `127.0.0.1:0` 失败; 同一命令在提升权限环境通过, 5 个反馈相关单测全部通过。
- 2026-05-06: `cargo test -p codex-app-transfer --no-fail-fast` 在提升权限环境通过, 16 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo check -p codex-app-transfer --all-targets` 通过。
- 2026-05-06: PR #20 初次远端 CI 中 `Rust · fast workspace check` 通过; `Rust · Tauri (src-tauri) check` 在安装 Linux Tauri 系统依赖阶段 8 分钟超时, 日志未显示业务代码编译或测试失败。
- 2026-05-06: PR #20 最终远端 CI 通过: `Detect changed paths` 通过, `Rust · fast workspace check (no Tauri sysdeps)` 通过, `Rust · Tauri (src-tauri) check` 通过。Tauri job 总耗时 9 分钟, 主要耗时仍为 Linux 系统依赖安装。
- 2026-05-06: `cargo check -p codex-app-transfer --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer update_ -- --nocapture` 在普通沙箱下因本地 latest.json/installer mock 需要监听 `127.0.0.1:0` 失败; 同一命令在提升权限环境通过, 4 个更新相关单测全部通过。
- 2026-05-06: `cargo test -p codex-app-transfer --no-fail-fast` 在提升权限环境通过, 20 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: 提交 `P2.4` 前复核 PR #22 远端检查, head `1cf0b68` 的 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过; `copilot` check 仍在运行且暂无审查输出可处理。
- 2026-05-06: `cargo check -p codex-app-transfer --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer startup_ -- --nocapture` 在提升权限环境通过, 2 个启动生命周期相关单测全部通过。
- 2026-05-06: `cargo test -p codex-app-transfer provider_switch_syncs_desktop_without_proxy_for_direct_provider -- --nocapture` 通过, 覆盖 provider 切换后的 direct provider 同步路径。
- 2026-05-06: `cargo test -p codex-app-transfer desktop_config_target -- --nocapture` 通过, 覆盖 local proxy/direct provider 目标选择。
- 2026-05-06: `cargo test -p codex-app-transfer --no-fail-fast` 在提升权限环境通过, 24 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: `cargo check -p codex-app-transfer --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer version_endpoint_matches_legacy_shape -- --nocapture` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer --no-fail-fast` 在提升权限环境通过, 25 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: `cargo check -p codex-app-transfer --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer desktop_ -- --nocapture` 通过, 5 个 Desktop 相关单测覆盖当前 GPT 模型槽位、status 输出、base URL mismatch 和 provider 切换同步路径。
- 2026-05-06: `node --check frontend/js/api.js` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer --no-fail-fast` 在普通沙箱下因既有本地 mock 测试和启动代理测试需要监听 `127.0.0.1:0` 失败; 同一命令在提升权限环境通过, 28 个 Tauri admin handler 单测全部通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: P1.3 远端 CI 最终通过: `Detect changed paths` 通过, `Rust · fast workspace check (no Tauri sysdeps)` 通过, `Rust · Tauri (src-tauri) check` 通过。
- 2026-05-06: P1.4 远端 CI 最终通过: `Detect changed paths` 通过, `Rust · fast workspace check (no Tauri sysdeps)` 通过, `Rust · Tauri (src-tauri) check` 通过, `copilot` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters responses:: -- --nocapture` 通过, 51 个 Responses 模块单测通过。
- 2026-05-06: `cargo check -p codex-app-transfer-adapters --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters --no-fail-fast` 通过, 60 个 adapter 单测和 3 个 integration 测试通过。
- 2026-05-06: `cargo test -p codex-app-transfer-proxy --lib` 通过, 19 个 proxy 库单测通过。
- 2026-05-06: `cargo check --workspace --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-proxy` 在普通沙箱下因 integration tests 需要监听 `127.0.0.1:0` 失败; 同一命令在提升权限环境通过, 19 个库单测、9 个 auth/routing 集成测试和 2 个 streaming passthrough 测试全部通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters responses:: -- --nocapture` 通过, 57 个 Responses 模块单测通过。
- 2026-05-06: `cargo check -p codex-app-transfer-adapters --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters --no-fail-fast` 通过, 66 个 adapter 单测和 3 个 integration 测试通过。
- 2026-05-06: `cargo check --workspace --all-targets` 通过。
- 2026-05-06: v2.0.2 release 准备本地验证: `cargo fmt --check` 通过, `git diff --check` 通过, `cargo check -p codex-app-transfer --all-targets` 通过。`cargo check` 期间 Xcode SDK 查询输出 macOS cache / fs event 警告, 但编译检查最终成功。
- 2026-05-06: PR #22 的 `P2.1` / `P2.2` 认领标记远端 CI 通过: `Detect changed paths` 通过, `Rust · fast workspace check (no Tauri sysdeps)` 通过, `Rust · Tauri (src-tauri) check` 通过。GitHub Actions 仅返回 Node.js 20 action deprecation warning, 与本次代码无关且不阻塞。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters responses::converter::tests -- --nocapture` 通过, 20 个 Chat SSE converter 单测通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters responses:: -- --nocapture` 通过, 59 个 Responses 模块单测通过。
- 2026-05-06: `cargo check -p codex-app-transfer-adapters --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-adapters --no-fail-fast` 通过, 68 个 adapter 单测和 3 个 integration 测试通过。
- 2026-05-06: `cargo check --workspace --all-targets` 通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: `rg -n "功能与 v1\\.0\\.4 完全兑现|all v1\\.0\\.4 functionality preserved|当前版本|Current version|平台:|Platforms:|stub|stubs" README.md docs/release-notes-v2.0.0.md` 通过人工复核, 已无"功能完全兑现"旧表述, README 版本/平台口径与仓库版本源一致。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: 提交 `P2.3` 前复核 PR #22 远端检查, head `2a89074` 的 `Detect changed paths`、`Rust · fast workspace check (no Tauri sysdeps)`、`Rust · Tauri (src-tauri) check` 均通过。
- 2026-05-06: `cargo test -p codex-app-transfer-registry model_alias::tests -- --nocapture` 通过, 7 个 model_alias 单测通过。
- 2026-05-06: `cargo test -p codex-app-transfer-proxy resolver::tests -- --nocapture` 通过, 14 个 resolver 单测通过。
- 2026-05-06: `cargo test -p codex-app-transfer-registry --no-fail-fast` 通过, 13 个 registry 单测和 7 个 golden compatibility 测试通过。
- 2026-05-06: `cargo test -p codex-app-transfer-proxy --lib` 通过, 23 个 proxy 库单测通过。
- 2026-05-06: `cargo check -p codex-app-transfer-proxy --all-targets` 通过。
- 2026-05-06: `cargo test -p codex-app-transfer-proxy` 在普通沙箱下因 integration tests 需要监听 `127.0.0.1:0` 失败; 同一命令在提升权限环境通过, 23 个库单测、9 个 auth/routing 集成测试和 2 个 streaming passthrough 测试全部通过。
- 2026-05-06: `cargo check --workspace --all-targets` 通过。
- 2026-05-06: `cargo fmt --check` 通过。
- 2026-05-06: `git diff --check` 通过。
- 2026-05-06: P3.2 文档静态校验通过: `git diff --check` 通过, `cargo fmt --check` 通过, `rg -n "responses_websocket|WebSocket 流|WebSocket stream|WebSocket.*主连接|primary connection" README.md crates docs/api-route-status.md` 无残留匹配。
- 2026-05-06: 提交 `P3.2` 前复核 PR #23 远端检查, head `95944aa` 的 `Detect changed paths` 和 `Rust · fast workspace check (no Tauri sysdeps)` 均通过, `Rust · Tauri (src-tauri) check` 为 docs-only skipped。
- 2026-05-06: 撤回 Rust 源码注释改动后复核最终 P3 diff: `git diff --name-only origin/main` 仅剩 README、docs 和推进文档。

## 当前阻塞

- 暂无代码实现阻塞。
- P0/P1.1/P1.2 的最终验收仍需要在允许外网请求、具备可用 API key、且可运行 Tauri 桌面窗口的环境中做实机验证: P0.1 真实 provider 模型列表拉取和映射写入、P0.2 真实 provider 余额/用量查询和真实认证失败路径、P0.3 前端备份/导入按钮实机点击、P0.4 打开日志目录的实机 UI 点击、P0.5 真实 Cloudflare Worker 收件结果、P1.1 真实 GitHub release latest.json 拉取和 macOS/Windows 安装器启动、P1.2 托盘 provider 切换和退出恢复的实机 UI 行为。

## 下一步

提交 `P3.2` 前先处理 `P3.1` 的远端审查与 CI 结果; 提交并更新 PR #23 后评论 `@copilot` 请求审查。P3 已是最后一节, PR #23 通过 review / CI 后按整节合并。
